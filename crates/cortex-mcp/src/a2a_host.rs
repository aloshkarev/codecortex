//! Graph-backed A2A role helpers — see [`crate::intelligence`].

pub use crate::intelligence::{
    compute_api_contract, compute_impact_graph, compute_patch_context, compute_test_context,
    impact_summary_for_a2a, patch_capsule_from_data, resolve_symbol,
};
